import React from 'react';import {render} from 'react-dom';import Reflux from 'reflux';//Import Componentsimport AudioFilesPanel from './components/AudioFilesPanel.jsx';import CalibrateStore from './stores/CalibrateStore.js';var CalibrateApp = React.createClass({  mixins: [Reflux.connect(CalibrateStore)],  getInitialState: function() {    return CalibrateStore.getDefaultData();  },  render () {    return (      <div className="container">        <div className="row centered">          <h1>Audicus</h1>        </div>        <div className="row centered">          <AudioFilesPanel settings={this.state.settings} inputOptions={this.state.inputOptions}/>        </div>      </div>   );  }});render(<CalibrateApp/>, document.getElementById('calibrate'));